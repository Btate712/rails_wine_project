<h1><%= @variety.name %></h1>
<p>Color: <%= @variety.color %></p>
<p>Wines:</p>
<ul>
  <% @wines.each do |wine| %>
    <li><%= link_to "#{wine.name} - #{wine.average_rating} stars", wine_path(wine) %></li>
  <% end %>
</ul>
